export enum AlertTypes {
  INFO = 'info',
  DANGER = 'danger',
  SUCCESS = 'success',
  WARNING = 'warning',
  DEFAULT = 'default',
}

export enum SvgIcons {
  send = 'send',
  follow = 'right',
  alert = 'alert',
  linkedin = 'linkedin',
  github = 'github',
}

export enum DomainCheckTypes {
  MX = 'mx',
  LOOKUP = 'lookup',
  NS = 'ns',
}
