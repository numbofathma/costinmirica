export enum AlertTypes {
  INFO = 'info',
  DANGER = 'danger',
  SUCCESS = 'success',
  WARNING = 'warning',
  DEFAULT = 'default',
}

export enum SvgIcons {
  plane = 'plane',
  follow = 'follow',
  alert = 'alert',
}
