import styled from "styled-components";

export const ParticlesWrapper = styled.div`
    background-color: #FF6600;

    canvas {
        width: 100%
        height: 100vh;
        z-index: -10;
        position: fixed;
        top: 0px;
    }
`;